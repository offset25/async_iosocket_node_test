function my_function(data, result) {

	// do something (it'll take sometime, you can use setTimeout() here, so that delay the time, for example.

	result.folder3_test1 = 'test1';
	return result;
}

module.exports = my_function;
